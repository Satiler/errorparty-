# 🚀 Развертывание ErrorParty на роутере

## Подготовка

1. **Включите SSH на роутере:**
   - Откройте http://192.168.31.1
   - Войдите (admin/admin)
   - Настройки → SSH → Включить

2. **Подключите SSD к роутеру** (отформатированный в ext4)

## Развертывание

### Вариант 1: Через SSH (рекомендуется)

```bash
# 1. Скопируйте файлы на роутер
scp -r router-deploy/* admin@192.168.31.1:/mnt/sda1/errorparty/

# 2. Подключитесь к роутеру
ssh admin@192.168.31.1

# 3. Перейдите в папку
cd /mnt/sda1/errorparty

# 4. Настройте переменные окружения
cp .env.example .env
vi .env  # или nano .env

# 5. Сделайте скрипт исполняемым
chmod +x deploy.sh

# 6. Запустите развертывание
./deploy.sh
```

### Вариант 2: Через веб-интерфейс роутера

1. Откройте http://192.168.31.1
2. Файловый менеджер → Загрузите все файлы из `router-deploy/`
3. Терминал → Выполните команды из deploy.sh вручную

## Структура файлов

```
router-deploy/
├── backend.tar          # Docker образ backend (107 MB)
├── frontend.tar         # Docker образ frontend (22 MB)
├── postgres.tar         # Docker образ PostgreSQL (104 MB)
├── redis.tar            # Docker образ Redis (16 MB)
├── nginx.tar            # Docker образ Nginx (22 MB)
├── docker-compose.yml   # Конфигурация Docker Compose
├── nginx.conf           # Конфигурация Nginx
├── .env.example         # Пример переменных окружения
└── deploy.sh            # Скрипт развертывания
```

## После развертывания

Откройте браузер: **http://192.168.31.1**

## Управление

```bash
# Просмотр логов
docker-compose logs -f

# Перезапуск
docker-compose restart

# Остановка
docker-compose down

# Обновление образа
docker load -i new-backend.tar
docker-compose up -d backend
```

## Требования

- Роутер с Docker
- SSD подключен и смонтирован в `/mnt/sda1/`
- Минимум 512 MB RAM
- ~300 MB места для образов
