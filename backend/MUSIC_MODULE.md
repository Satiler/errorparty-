# 🎵 Музыкальный модуль ErrorParty

Кроссплатформенный музыкальный модуль с ML-рекомендациями, загрузкой треков, избранным и интеграцией с мировыми музыкальными сервисами.

## 📋 Возможности

### ✅ Реализовано

- ✨ **Загрузка музыки** - поддержка mp3, flac, wav, m4a, ogg
- 📊 **Извлечение метаданных** - автоматическое определение исполнителя, названия, жанра
- 🎧 **Стриминг** - потоковое воспроизведение с поддержкой range requests
- ⬇️ **Скачивание** - возможность скачивать треки (с учетом прав)
- ❤️ **Избранное** - добавление треков в понравившиеся
- 📜 **История** - запись прослушиваний для ML
- 📂 **Плейлисты** - создание и управление плейлистами
- 🤖 **ML Рекомендации**:
  - Collaborative Filtering (похожие пользователи)
  - Content-Based (жанры, исполнители)
  - Audio Features (tempo, energy, valence)
- 🔍 **Поиск** - по трекам, исполнителям, альбомам, жанрам
- 🌐 **Внешние интеграции**:
  - Spotify API (поиск)
  - YouTube Music API (поиск)
- 📈 **Статистика** - топ треки, статистика пользователя
- 🔌 **Socket.IO** - real-time обновления плеера

## 🗂️ Структура

```
backend/src/modules/music/
├── index.js              # Регистрация модуля
├── music.routes.js       # API endpoints
├── music.controller.js   # Бизнес-логика
├── music.service.js      # ML алгоритмы и интеграции
└── music.middleware.js   # Multer для загрузки

backend/src/models/
├── Track.js              # Модель трека
├── TrackLike.js          # Лайки
├── ListeningHistory.js   # История прослушиваний
├── Playlist.js           # Плейлисты
└── PlaylistTrack.js      # Треки в плейлистах

backend/uploads/
├── music/                # Загруженные треки
└── covers/               # Обложки альбомов
```

## 🚀 Установка

### 1. Установить зависимости

```bash
cd backend
npm install multer music-metadata axios
```

### 2. Применить миграцию БД

```bash
npx sequelize-cli db:migrate
```

### 3. Добавить переменные окружения

Добавить в `backend/.env`:

```env
# Spotify API (опционально)
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret

# YouTube API (опционально)
YOUTUBE_API_KEY=your_youtube_api_key
```

### 4. Пересобрать Docker

```bash
docker-compose up -d --build backend
```

## 📡 API Endpoints

### Треки

- `GET /api/music/tracks` - Список треков (пагинация, фильтры)
- `GET /api/music/tracks/:id` - Информация о треке
- `POST /api/music/tracks/upload` - Загрузить трек (требует auth)
- `GET /api/music/tracks/:id/stream` - Стриминг трека
- `GET /api/music/tracks/:id/download` - Скачать трек (требует auth)
- `DELETE /api/music/tracks/:id` - Удалить трек (требует auth)

### Избранное

- `POST /api/music/tracks/:id/like` - Лайк трека (требует auth)
- `DELETE /api/music/tracks/:id/like` - Убрать лайк (требует auth)
- `GET /api/music/favorites` - Избранные треки (требует auth)

### История

- `POST /api/music/tracks/:id/listen` - Записать прослушивание (требует auth)
- `GET /api/music/history` - История прослушиваний (требует auth)

### Плейлисты

- `GET /api/music/playlists` - Плейлисты пользователя (требует auth)
- `POST /api/music/playlists` - Создать плейлист (требует auth)
- `GET /api/music/playlists/:id` - Треки плейлиста
- `POST /api/music/playlists/:id/tracks` - Добавить трек (требует auth)
- `DELETE /api/music/playlists/:id/tracks/:trackId` - Удалить трек (требует auth)
- `DELETE /api/music/playlists/:id` - Удалить плейлист (требует auth)

### Рекомендации

- `GET /api/music/recommendations` - Персональные рекомендации (требует auth)
- `GET /api/music/recommendations/similar/:trackId` - Похожие треки

### Поиск

- `GET /api/music/search?q=query` - Поиск треков
- `GET /api/music/genres` - Список жанров
- `GET /api/music/genres/:genre/tracks` - Треки по жанру

### Внешние интеграции

- `GET /api/music/external/search?q=query&source=spotify` - Поиск в Spotify/YouTube
- `POST /api/music/external/import` - Импорт трека (в разработке)

### Статистика

- `GET /api/music/stats/top?period=week` - Топ треки
- `GET /api/music/stats/user` - Статистика пользователя (требует auth)

## 🎨 Примеры использования

### Загрузка трека

```javascript
const formData = new FormData();
formData.append('track', audioFile);
formData.append('cover', coverFile); // опционально
formData.append('title', 'My Song');
formData.append('artist', 'Artist Name');
formData.append('genre', 'Rock');
formData.append('isPublic', 'true');
formData.append('allowDownload', 'true');

const response = await fetch('/api/music/tracks/upload', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${token}`
  },
  body: formData
});
```

### Стриминг трека

```html
<audio controls src="/api/music/tracks/123/stream"></audio>
```

### Получение рекомендаций

```javascript
const response = await fetch('/api/music/recommendations?limit=20', {
  headers: {
    'Authorization': `Bearer ${token}`
  }
});
const data = await response.json();
console.log(data.tracks); // Персональные рекомендации
```

## 🤖 ML Алгоритмы

### Collaborative Filtering

Находит пользователей с похожими вкусами и рекомендует треки, которые они слушают.

**Алгоритм:**
1. Анализ истории прослушиваний и лайков текущего пользователя
2. Поиск пользователей с максимальным пересечением треков
3. Рекомендация треков, которые они слушали, но текущий пользователь - нет

### Content-Based Filtering

Рекомендует треки на основе жанров и исполнителей, которые нравятся пользователю.

**Алгоритм:**
1. Анализ топ жанров и исполнителей пользователя
2. Поиск треков с этими характеристиками
3. Сортировка по популярности

### Audio Features Similarity

Находит похожие треки на основе аудио-характеристик.

**Фичи:**
- `tempo` - BPM (удары в минуту)
- `energy` - энергичность (0-1)
- `valence` - позитивность (0-1)
- `danceability` - танцевальность (0-1)
- `acousticness` - акустичность (0-1)
- `instrumentalness` - инструментальность (0-1)

**Метод:** Euclidean Distance между векторами фичей

## 🔒 Безопасность

- JWT аутентификация для приватных операций
- Проверка прав доступа к приватным трекам
- Валидация форматов файлов
- Ограничение размера файлов (50 MB для аудио)
- Rate limiting на API endpoints

## 📝 TODO

### В разработке:

- [ ] **Frontend компоненты**:
  - [ ] MusicPlayer с waveform
  - [ ] TrackList с виртуализацией
  - [ ] UploadModal
  - [ ] RecommendationsCarousel
  - [ ] PlaylistManager
  
- [ ] **Извлечение обложек** из MP3 tags
  
- [ ] **Real-time аудио фичи** с FFT анализом (meyda, essentia.js)
  
- [ ] **Импорт из внешних источников** (требует правовую проверку)
  
- [ ] **Waveform generation** для визуализации треков
  
- [ ] **Lyrics** интеграция с Genius API
  
- [ ] **Social features**:
  - [ ] Комментарии к трекам
  - [ ] Sharing плейлистов
  - [ ] Collaborative playlists
  
- [ ] **Advanced ML**:
  - [ ] Neural Networks для рекомендаций
  - [ ] User embedding vectors
  - [ ] Real-time персонализация

### Улучшения безопасности:

- [ ] DRM защита для треков
- [ ] Watermarking загруженных файлов
- [ ] Copyright detection (audio fingerprinting)
- [ ] DMCA compliance система

## 📚 Полезные ссылки

- [Spotify Web API](https://developer.spotify.com/documentation/web-api/)
- [YouTube Data API](https://developers.google.com/youtube/v3)
- [music-metadata](https://github.com/Borewit/music-metadata)
- [Meyda Audio Features](https://github.com/meyda/meyda)
- [Collaborative Filtering](https://en.wikipedia.org/wiki/Collaborative_filtering)

## 🤝 Вклад

Модуль создан для ErrorParty.ru и готов к расширению!

**Автор:** ErrorParty Dev Team  
**Дата:** 29 ноября 2025
