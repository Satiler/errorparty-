# 🎵 Система скачивания и декодирования аудио - Docker

Улучшенная мульти-сервисная система для скачивания и декодирования треков из популярных источников в Docker окружении.

## ✨ Возможности

### 🔓 Мульти-сервисное декодирование
- **KissVK** - base64/hex декодирование
- **VK Audio** - VK crypto алгоритм
- **Yandex Music** - проверка подписанных URL
- **Generic** - универсальный обработчик

### ⬇️ Умное скачивание
- Retry с exponential backoff (до 3 попыток)
- Валидация аудио файлов (magic bytes)
- Кэширование скачанных файлов
- Batch операции с ограничением concurrency
- Автоматическая очистка старых файлов

### 🐳 Docker интеграция
- Работа с Docker volumes
- Персистентное хранилище
- Оптимизация для контейнера
- Поддержка volume монтирования

## 📁 Структура

```
backend/
  src/
    services/
      audio-decoder.service.js      # Декодирование URL
      audio-downloader.service.js   # Скачивание файлов
      kissvk.service.js             # KissVK интеграция
  test-audio-system-docker.js       # Тест в Docker
```

## 🚀 Использование в Docker

### 1. Переменные окружения

В `docker-compose.yml`:

```yaml
environment:
  - AUDIO_DOWNLOAD_PATH=/app/uploads/audio
```

### 2. Volume монтирование

Файлы сохраняются в Docker volume:

```yaml
volumes:
  - uploads:/app/uploads
```

Доступ к файлам:
- **Внутри контейнера**: `/app/uploads/audio/`
- **Через Nginx**: `http://localhost/uploads/audio/`

### 3. Запуск теста

```bash
# Внутри Docker контейнера
docker-compose exec backend node test-audio-system-docker.js
```

## 💻 API примеры

### Декодирование URL

```javascript
const { getInstance } = require('./src/services/audio-decoder.service');
const decoder = getInstance();

// Автоматическое определение сервиса
const result = await decoder.decode(url, { userId, audioId });

// Результат
{
  success: true,
  url: 'https://decoded-url.mp3',
  decoded: true,
  method: 'kissvk',
  format: 'mp3'
}
```

### Скачивание файла

```javascript
const { getInstance } = require('./src/services/audio-downloader.service');
const downloader = getInstance();

const result = await downloader.download(url, {
  filename: 'artist_-_title.mp3',
  metadata: { artist, title, source: 'kissvk' },
  validate: true
});

// Результат
{
  success: true,
  filepath: '/app/uploads/audio/artist_-_title.mp3',
  filename: 'artist_-_title.mp3',
  size: 3456789,
  url: '/uploads/audio/artist_-_title.mp3',
  cached: false
}
```

### Batch скачивание

```javascript
const tracks = [
  { url: 'https://...', filename: 'track1.mp3', metadata: {...} },
  { url: 'https://...', filename: 'track2.mp3', metadata: {...} }
];

const results = await downloader.downloadBatch(tracks, {
  concurrency: 3,
  continueOnError: true
});
```

## 🔧 Управление файлами

### Список файлов

```javascript
const files = await downloader.listFiles();
// Возвращает массив файлов с метаданными
```

### Информация о файле

```javascript
const info = await downloader.getFileInfo('track.mp3');
// { exists, filepath, size, metadata, url, ... }
```

### Очистка старых файлов

```javascript
await downloader.cleanup(30); // Удалить файлы старше 30 дней
```

## 🎯 Поддерживаемые сервисы

| Сервис | Декодирование | Скачивание | Формат |
|--------|---------------|------------|---------|
| KissVK | ✅ base64/hex | ✅ | MP3 |
| VK Audio | ✅ crypto | ✅ | MP3 |
| Yandex Music | ✅ signature | ✅ | MP3 |
| Generic | ⚠️ direct | ✅ | MP3/Any |

## 📊 Тестирование

Тестовый скрипт проверяет:
1. ✅ Docker окружение и пути
2. ✅ Извлечение треков с KissVK
3. ✅ Декодирование URL
4. ✅ Скачивание и валидация
5. ✅ Batch операции
6. ✅ Список файлов
7. ✅ Определение сервисов

```bash
docker-compose exec backend node test-audio-system-docker.js
```

## 🔐 Безопасность

- Валидация файлов через magic bytes
- Ограничение размера файлов (50 MB)
- Timeout на запросы (30 сек)
- Очистка временных файлов
- Санитизация имен файлов

## ⚡ Производительность

- **Concurrency**: 3 одновременных загрузки
- **Retry**: Exponential backoff (1s → 2s → 4s)
- **Cache**: Проверка существующих файлов
- **Stream**: Потоковая запись файлов
- **Cleanup**: Автоматическая очистка

## 🐛 Обработка ошибок

Все операции включают:
- Try-catch обертки
- Подробное логирование
- Graceful fallback
- Детальные error messages

## 📝 Метаданные

Для каждого файла сохраняется `.meta.json`:

```json
{
  "artist": "Artist Name",
  "title": "Track Title",
  "source": "kissvk",
  "downloadedAt": "2025-12-25T10:00:00.000Z",
  "filepath": "/app/uploads/audio/track.mp3"
}
```

## 🚦 Статусы

### Decode статусы
- `success: true` - декодирование успешно
- `decoded: true` - URL был изменен
- `method` - использованный метод (base64, hex, vk-crypto, etc)

### Download статусы
- `success: true` - скачивание успешно
- `cached: true` - файл уже существовал
- `size` - размер файла в байтах
- `validated` - файл прошел валидацию

## 🔗 Интеграция

Система легко интегрируется с:
- Существующим KissVK контроллером
- Playlist системой
- Music player frontend
- API endpoints

## 📌 Важные заметки

1. **Docker Volume**: Файлы сохраняются в `uploads` volume для персистентности
2. **Paths**: Используйте абсолютные пути внутри контейнера
3. **Nginx**: Настройте проксирование для `/uploads/audio/`
4. **Cleanup**: Регулярно вызывайте `cleanup()` для очистки диска

## 🎓 Best Practices

1. Всегда проверяйте `success` перед использованием результата
2. Используйте `continueOnError: true` для batch операций
3. Включайте metadata для лучшей организации
4. Валидируйте файлы после скачивания
5. Регулярно очищайте старые файлы

## 🆘 Troubleshooting

### Файлы не скачиваются
- Проверьте volume монтирование
- Проверьте права доступа к `/app/uploads/audio`
- Проверьте логи: `docker-compose logs backend`

### URL не декодируется
- Убедитесь что сервис определен правильно
- Проверьте metadata (userId, audioId для VK)
- Попробуйте generic декодер

### Большой размер volume
- Запустите `cleanup(30)` для очистки
- Уменьшите retention период
- Настройте автоматическую очистку

---

**Создано**: 25.12.2025  
**Docker**: ✅ Оптимизировано  
**Production**: ✅ Ready
