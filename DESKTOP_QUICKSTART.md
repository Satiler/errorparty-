# 🚀 Быстрый запуск Desktop приложения

## 1️⃣ Установка Rust (единоразово)

```powershell
# Windows - PowerShell от администратора
Invoke-WebRequest -Uri https://win.rustup.rs -OutFile rustup-init.exe
.\rustup-init.exe -y

# Перезапустить терминал после установки!
```

**Проверка:**
```powershell
rustc --version
# Должно показать: rustc 1.75.0 (или новее)
```

## 2️⃣ Установка зависимостей

```powershell
# В корне проекта (D:\МОЙ САЙТ)
npm install
```

## 3️⃣ Запуск в режиме разработки

```powershell
# Вариант 1: Из корня проекта
npm run tauri:dev

# Вариант 2: Из frontend
cd frontend
npm run tauri:dev
```

**Что произойдёт:**
- ⚙️ Компиляция Rust (~2-3 минуты в первый раз)
- 🚀 Запуск Vite dev server
- 🪟 Открытие desktop окна

## 4️⃣ Сборка установщика Windows

```powershell
# Собрать production версию
npm run tauri:build
```

**Результат:**
```
src-tauri/target/release/bundle/nsis/
  └── ErrorParty_1.0.0_x64-setup.exe  (установщик ~5 МБ)
```

---

## ⚡ Возможности

✅ **Системный трей** - сворачивание вместо закрытия  
✅ **Медиа-клавиши** - Play/Pause/Next/Prev на клавиатуре  
✅ **Нативные уведомления** - "🎵 Сейчас играет..."  
✅ **Размер 3-5 МБ** - в 30 раз меньше Electron!

---

## 🐛 Проблемы?

**"rustc not found"** → Установить Rust (шаг 1)  
**"link.exe not found"** → Установить [Visual Studio Build Tools](https://visualstudio.microsoft.com/downloads/)  
**Backend недоступен** → Запустить `docker-compose up -d backend`

---

**Подробная документация:** [DESKTOP_APP_GUIDE.md](./DESKTOP_APP_GUIDE.md)
