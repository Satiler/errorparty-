# 🤖 Автоматическая Музыкальная Система - Быстрый старт

## Что это?

Система, которая **автоматически**:
- ✅ Скачивает популярные песни с KissVK, iTunes, Billboard
- ✅ Создаёт топовые плейлисты (Русские хиты, Мировые хиты, ТОП 100)
- ✅ Делает персональные подборки для каждого пользователя на основе его лайков
- ✅ Обновляет всё по расписанию без вашего участия

## Быстрый запуск

### 1. Автоматический режим (рекомендуется)

Просто запустите Docker контейнер - система запустится сама:
```bash
docker-compose up -d
```

Система будет работать по расписанию:
- 📥 **Каждые 3 часа** - импорт новых треков
- 📊 **Каждые 6 часов** - обновление плейлистов  
- 🎯 **Каждый день в 05:00** - персональные рекомендации
- 🔄 **Каждый день в 04:00** - полное обновление

### 2. Ручной запуск (в Docker контейнере)

#### Импортировать новые песни прямо сейчас
```bash
docker-compose exec backend node run-auto-music-system.js import
```

#### Обновить все плейлисты
```bash
docker-compose exec backend node run-auto-music-system.js playlists
```

#### Создать персональные подборки для всех пользователей
```bash
docker-compose exec backend node run-auto-music-system.js recommendations
```

#### Сделать всё сразу (импорт + плейлисты + рекомендации)
```bash
docker-compose exec backend node run-auto-music-system.js full
```

#### Посмотреть статус системы
```bash
docker-compose exec backend node run-auto-music-system.js status
```

## Что система создаёт?

### Автоматические плейлисты:

1. **🔥 ТОП 100 ХИТОВ** - самые популярные треки
2. **🇷🇺 РУССКИЕ ХИТЫ 2025** - лучшее из русской музыки
3. **🌍 МИРОВЫЕ ХИТЫ 2025** - топ зарубежных треков
4. **🆕 НОВИНКИ НЕДЕЛИ** - свежие песни последних 7 дней
5. **🎧 РЕДАКТОРСКИЙ ВЫБОР** - отборная музыка

### Персональные плейлисты:

**❤️ МНЕ ПОНРАВИТСЯ** - для каждого пользователя создаётся свой плейлист на основе:
- Жанров, которые он лайкает
- Любимых артистов
- Популярности треков

> **Примечание:** Нужно минимум 5 лайков, чтобы система создала персональный плейлист

## API для управления

### Посмотреть статус
```bash
# С хоста (Windows PowerShell)
curl http://localhost:3000/api/music/auto/status

# Или из контейнера
docker-compose exec backend curl http://localhost:3000/api/music/auto/status
```

### Запустить импорт
```bash
curl -X POST http://localhost:3000/api/music/auto/run/import
```

### Запустить обновление плейлистов
```bash
curl -X POST http://localhost:3000/api/music/auto/run/playlists
```

### Создать персональные рекомендации
```bash
curl -X POST http://localhost:3000/api/music/auto/run/recommendations
```

### Получить персональный плейлист пользователя
```bash
curl http://localhost:3000/api/music/auto/personal/1
```

### Создать персональный плейлист для конкретного пользователя
```bash
curl -X POST http://localhost:3000/api/music/auto/generate-personal/1
```

## Откуда берутся песни?

### ⚡ ВАЖНО: Все файлы скачиваются ТОЛЬКО с KissVK!

Другие платформы предоставляют только **информацию о популярных треках** (что сейчас в топе).

### Схема работы:

```
Яндекс.Музыка ──► Список популярных треков ──► KissVK ──► Скачивание файлов
iTunes          ──► Список популярных треков ──► KissVK ──► Скачивание файлов  
Billboard       ──► Список популярных треков ──► KissVK ──► Скачивание файлов
```

### Источники информации о популярных треках:

#### Российские чарты 🇷🇺
- **Яндекс.Музыка** - топ российских треков
  - Инстасамка, Miyagi, Скриптонит, Три дня дождя и т.д.
  - Файлы скачиваются с KissVK

#### Зарубежные чарты 🌍
- **iTunes** - официальный Top Charts API (Top 25)
- **Billboard** - Hot 100 чарт (Top 14)
  - Taylor Swift, Billie Eilish, Ariana Grande и т.д.
  - Файлы скачиваются с KissVK

#### Прямой импорт с KissVK 💋
- Top 100 чарт (50 треков)
- Русская музыка (30 треков)
- Новинки (20 треков)

**Итого:** ~100-150 новых треков за один цикл импорта, все файлы только с KissVK!

## Как работают персональные рекомендации?

1. **Анализ ваших лайков**
   - Какие жанры вы чаще всего лайкаете?
   - Какие артисты вам нравятся?
   - Какой уровень популярности треков вы предпочитаете?

2. **Поиск похожих треков**
   - Песни из ваших любимых жанров
   - Треки ваших любимых артистов
   - Треки с похожим уровнем популярности

3. **Диверсификация**
   - Система не даёт одинаковые треки
   - Исключает уже лайкнутые песни
   - Балансирует между разными жанрами

## Проверка работы

### 1. Проверьте, что Docker контейнер запущен
```bash
docker-compose ps
```

Должен быть статус `Up`

### 2. Проверьте, что система запущена
```bash
curl http://localhost:3000/api/music/auto/status
```

Должно быть `"isRunning": true`

### 3. Проверьте плейлисты в базе
Зайдите на ваш сайт в раздел "Плейлисты" - должны появиться новые автоматические плейлисты

### 4. Проверьте логи Docker контейнера
```bash
docker-compose logs backend | Select-String "Automated Music System"
```

В логах должны быть сообщения:
```
🤖 Initializing Automated Music System...
✅ Automated Music System started!
```

## Что делать если не работает? в Docker
```bash
docker-compose exec backend node run-auto-music-system.js import
```

**Решение 2:** Проверьте доступность KissVK из контейнера
```bash
docker-compose exec backend curl https://kissvk.top/
```

### Проблема: Не создаются персональные плейлисты

**Причина:** У пользователей мало лайков (нужно минимум 5)

**Решение:** Попросите пользователей лайкнуть больше треков

### Проблема: Система не запускается

**Решение:** Проверьте логи Docker контейнера
```bash
docker-compose logs -f backend | Select-String "Music"
```

Или посмотрите все логи:
```bash
docker-compose logs -f backend

**Решение:** Проверьте логи
```bash
tail -f backend/logs/server.log | grep "Music"
```

## Настройка расписания

Если хотите изменить расписание, отредактируйте файл:
```
backend/src/schedulers/auto-music-system.scheduler.js
```

Формат времени (cro (Docker)

Все команды выполняются через `docker-compose exec backend`:

```bash
# Быстрый тест системы (1 песня)
docker-compose exec backend node run-auto-music-system.js test

# Полное обновление
docker-compose exec backend node run-auto-music-system.js full

# Только импорт
docker-compose exec backend node run-auto-music-system.js import

# Только плейлисты
docker-compose exec backend node run-auto-music-system.js playlists

# Только рекомендации
docker-compose exec backend node run-auto-music-system.js recommendations

# Статус
docker-compose exec backend node run-auto-music-system.js status

# Запустить планировщик отдельно
docker-compose exec backend node run-auto-music-system.js start
``` и пересоберите Docker образ:
```javascript
// Initialize Automated Music System (New!)
// try { ... } catch { ... }
```
Затем:
```bash
docker-compose down
docker-compose up -d --build
```bash
# Запустить все контейнеры
docker-compose up -d

# Остановить все контейнеры
docker-compose down

# Перезапустить backend
docker-compose restart backend

# Посмотреть логи
docker-compose logs -f backend

# Зайти в контейнер
docker-compose exec backend bash
# Статус
node backend/run-auto-music-system.js status

# Запустить планировщик отдельно
node backend/run-auto-music-system.js start
```

## FAQ

**Q: Сколько треков импортируется в день?**  
A: ~200-400 уникальных треков после удаления дубликатов

**Q: Можно ли отключить автоматический импорт?**  
A: Да, закомментируйте секцию в `backend/src/server.js`:
```javascript
// Initialize Automated Music System (New!)
// try { ... } catch { ... }
```

**Q: Как часто обновляются персональные плейлисты?**  
A: Каждый день в 05:00

**Q: Можно ли добавить свои источники?**  
A: Да, добавьте их в `backend/src/services/auto-music-system.service.js`

**Q: Система потребляет много ресурсов?**  
A: Нет, импорт занимает ~1-2 минуты каждые 3 часа

---

**Готово!** 🎉 Теперь ваш музыкальный сервис будет автоматически пополняться свежими треками и создавать крутые плейлисты!

Если есть вопросы - пишите в Issues на GitHub или в поддержку.
