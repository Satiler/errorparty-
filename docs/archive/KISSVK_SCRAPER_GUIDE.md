# KissVK.top Scraper — Руководство

Puppeteer-скрейпер для извлечения треков с **kissvk.top** (VK Music зеркало) и импорта в базу данных через API.

---

## 📋 Содержание

1. [Установка](#установка)
2. [Быстрый старт](#быстрый-старт)
3. [Скрейпинг треков](#скрейпинг-треков)
4. [Импорт в базу](#импорт-в-базу)
5. [Примеры использования](#примеры-использования)
6. [Устранение неполадок](#устранение-неполадок)

---

## 🚀 Установка

### Требования

- **Node.js** 18+ (проверка: `node -v`)
- **npm** или **yarn**
- **Puppeteer** (уже должен быть установлен для sefon-scraper)

### Проверка установки

```powershell
# Переход в папку проекта
cd "d:\МОЙ САЙТ"

# Puppeteer уже установлен?
node -e "console.log(require('puppeteer').version())"
```

---

## ⚡ Быстрый старт

### 1. Скрейпинг топ-20 треков

```powershell
node tools/kissvk-scraper.js --url=/tracks_chart --limit=20 --output=kissvk-tracks.json
```

**Результат:** файл `kissvk-tracks.json` с 20 треками из чарта

### 2. Импорт в базу (dry-run)

```powershell
node tools/kissvk-import.js --input=kissvk-tracks.json --dry-run
```

**Результат:** проверка данных без отправки на сервер

### 3. Реальный импорт

```powershell
node tools/kissvk-import.js --input=kissvk-tracks.json --api=http://localhost:3001/api
```

**Результат:** треки добавлены в базу через `POST /api/music/tracks`

---

## 🎵 Скрейпинг треков

### Команда

```powershell
node tools/kissvk-scraper.js [опции]
```

### Опции

| Опция              | Описание                                      | По умолчанию       |
|--------------------|-----------------------------------------------|--------------------|
| `--url=<path>`     | Путь страницы для скрейпинга                  | `/tracks_chart`    |
| `--limit=<число>`  | Максимальное количество треков                | `50`               |
| `--output=<файл>`  | Путь для сохранения JSON (если не указан — вывод в консоль) | `null` |
| `--no-headless`    | Запустить браузер с видимым окном             | `false`            |
| `--timeout=<мс>`   | Таймаут загрузки страницы                     | `60000`            |
| `--wait=<мс>`      | Время ожидания декодирования треков           | `5000`             |

### Примеры

#### Скрейпинг чарта треков

```powershell
# Топ-50 треков
node tools/kissvk-scraper.js --url=/tracks_chart --limit=50 --output=chart.json

# Топ-100 с увеличенным временем декодирования
node tools/kissvk-scraper.js --url=/tracks_chart --limit=100 --wait=10000 --output=top100.json
```

#### Скрейпинг чарта альбомов

```powershell
node tools/kissvk-scraper.js --url=/albums_chart --limit=20 --output=albums-chart.json
```

#### Скрейпинг конкретного альбома/плейлиста

```powershell
# SLAANG альбом (Aarne, Toxi$)
node tools/kissvk-scraper.js --url=/playlist-2000753343_25753343_ce3a98a09f3a21c9e0 --output=slaang.json

# QUATTRO: La Famiglia (ALBLAK 52)
node tools/kissvk-scraper.js --url=/playlist-2000824575_25824575_989728f760ea90fdb1 --output=quattro.json
```

#### Скрейпинг новых альбомов

```powershell
node tools/kissvk-scraper.js --url=/new_albums --limit=30 --output=new-albums.json
```

#### Скрейпинг подборок

```powershell
# Музыкальные подборки
node tools/kissvk-scraper.js --url=/collections --limit=20 --output=collections.json

# Выбор редакции
node tools/kissvk-scraper.js --url=/editors_choice --limit=30 --output=editors-choice.json
```

#### Скрейпинг с видимым браузером (для отладки)

```powershell
node tools/kissvk-scraper.js --url=/tracks_chart --limit=10 --no-headless
```

### Структура JSON

```json
[
  {
    "trackId": "-2001963489_143963489",
    "artist": "Jakone, Kiliana",
    "title": "Жиганская",
    "duration": "2:09",
    "durationSeconds": 129,
    "chartPosition": 1,
    "streamUrl": "https://cdn9.sefon.pro/prev/...",
    "encodedAudio": null,
    "coverUrl": "https://sun9-6.userapi.com/...",
    "source": "kissvk.top",
    "scrapedAt": "2025-12-04T12:34:56.789Z"
  }
]
```

### Важные замечания

1. **JS-декодер:** Скрипт ждёт 5 секунд (по умолчанию) после загрузки страницы для расшифровки `data-audio` атрибутов
2. **Без streamUrl:** Если `streamUrl === null`, треки имеют `encodedAudio` — зашифрованную ссылку
3. **Увеличение времени ожидания:** Используйте `--wait=10000` для медленного интернета
4. **VK CDN:** Ссылки ведут на официальные CDN серверы VK (через sefon.pro прокси)

---

## 📥 Импорт в базу

### Команда

```powershell
node tools/kissvk-import.js [опции]
```

### Опции

| Опция                  | Описание                                      | По умолчанию              |
|------------------------|-----------------------------------------------|---------------------------|
| `--input=<файл>`       | Путь к JSON с треками                         | `tracks.json`             |
| `--api=<url>`          | Базовый URL API                               | `http://localhost:3001/api` |
| `--endpoint=<path>`    | Endpoint для создания треков                  | `/music/tracks`           |
| `--dry-run`            | Сухой прогон (не отправляет запросы)          | `false`                   |
| `--concurrent=<число>` | Количество одновременных запросов             | `3`                       |
| `--delay=<мс>`         | Задержка между батчами                        | `500`                     |
| `--token=<jwt>`        | JWT токен авторизации                         | `null`                    |

### Примеры

#### Импорт с dry-run (проверка)

```powershell
node tools/kissvk-import.js --input=kissvk-tracks.json --dry-run
```

#### Реальный импорт

```powershell
# Локальный сервер
node tools/kissvk-import.js --input=kissvk-tracks.json --api=http://localhost:3001/api

# Продакшн сервер
node tools/kissvk-import.js --input=kissvk-tracks.json --api=https://errorparty.ru/api
```

#### Импорт с авторизацией

```powershell
node tools/kissvk-import.js --input=kissvk-tracks.json --token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

#### Быстрый импорт (больше запросов одновременно)

```powershell
node tools/kissvk-import.js --input=kissvk-tracks.json --concurrent=10 --delay=200
```

---

## 🔄 Полный пайплайн

### Сценарий 1: Импорт топ-50 треков

```powershell
# 1. Скрейпинг
node tools/kissvk-scraper.js --url=/tracks_chart --limit=50 --output=top50.json

# 2. Проверка (dry-run)
node tools/kissvk-import.js --input=top50.json --dry-run

# 3. Импорт в базу
node tools/kissvk-import.js --input=top50.json --api=http://localhost:3001/api
```

### Сценарий 2: Импорт нескольких альбомов

```powershell
# Альбом 1: SLAANG
node tools/kissvk-scraper.js --url=/playlist-2000753343_25753343_ce3a98a09f3a21c9e0 --output=album1.json
node tools/kissvk-import.js --input=album1.json

# Альбом 2: QUATTRO
node tools/kissvk-scraper.js --url=/playlist-2000824575_25824575_989728f760ea90fdb1 --output=album2.json
node tools/kissvk-import.js --input=album2.json

# Альбом 3: Судный день (ЛСП)
node tools/kissvk-scraper.js --url=/playlist-2000953339_25953339_588949dce47b6db0b5 --output=album3.json
node tools/kissvk-import.js --input=album3.json
```

---

## 🔧 Технические детали

### Как работает декодирование

1. **Puppeteer** загружает страницу kissvk.top
2. Браузер автоматически выполняет `/js/cr.js` и `/js/cr-format.js`
3. JavaScript на странице расшифровывает `data-audio` атрибуты
4. Скрипт извлекает уже расшифрованные ссылки (если доступны)

### Формат data-audio

```html
<div class="audio" 
  data-id="-2001963489_143963489"
  data-audio="зашифрованная_строка_base64"
  data-cover="https://...">
```

После декодирования:
```
streamUrl: "https://cdn9.sefon.pro/prev/.../track.mp3"
```

### Почему нужно ждать декодирования?

- KissVK.top использует **клиентский JS-декодер**
- Декодер загружается асинхронно
- Требуется время на расшифровку всех треков на странице
- `--wait=5000` (по умолчанию) достаточно для большинства случаев

### Что если streamUrl === null?

Трек содержит `encodedAudio` — зашифрованную строку. Возможные причины:
1. Декодер не успел выполниться (увеличьте `--wait`)
2. Функция `cr()` не найдена на странице
3. Формат шифрования изменился

**Решение:** Увеличьте время ожидания или используйте `--no-headless` для отладки

---

## ❓ Возможные проблемы

### "Треков с расшифрованным URL: 0"

- **Причина:** Декодер не успел выполниться
- **Решение:** Увеличьте `--wait=10000` или больше

### "Timeout loading page"

- **Причина:** Медленный интернет или сайт недоступен
- **Решение:** Увеличьте `--timeout=120000` (2 минуты)

### Треки пропускаются при импорте

- **Причина:** `streamUrl === null` (не расшифрован)
- **Решение:** Повторите скрейпинг с увеличенным `--wait`

### Ошибки 403/429 при импорте

- **Причина:** Слишком много запросов к API
- **Решение:** Уменьшите `--concurrent=1` и увеличьте `--delay=1000`

---

## 🆚 Сравнение с Sefon.pro

| Параметр              | KissVK.top                  | Sefon.pro                   |
|-----------------------|-----------------------------|-----------------------------|
| Источник              | VK Music (зеркало)          | Sefon собственная база      |
| Качество              | VK Audio (обычно 192kbps)   | 192kbps MP3                 |
| Декодирование         | Требуется (JS на клиенте)   | Прямые ссылки               |
| Чарты                 | ✅ VK Music чарты           | ✅ Sefon чарты              |
| Альбомы               | ✅ Полные альбомы VK        | ✅ Альбомы                  |
| Скорость скрейпинга   | Медленнее (нужно ждать JS)  | Быстрее (прямые ссылки)     |
| Стабильность ссылок   | VK CDN (стабильные)         | Sefon CDN (стабильные)      |

---

## 📝 Примечания

- **Легальность:** Используйте только для личных целей или с разрешением правообладателей
- **Rate Limiting:** Не забывайте про задержки между запросами
- **Прокси:** Можно добавить поддержку прокси в Puppeteer (аналогично sefon-scraper)
- **Кэширование:** Рекомендуется кэшировать JSON результаты

---

## 🎯 Что дальше?

1. ✅ **Создан скрейпер kissvk-scraper.js**
2. ✅ **Создан импортер kissvk-import.js**
3. 🔄 **Тестирование на реальных данных**
4. 🚀 **Интеграция с основным приложением**

---

**Готово к использованию!** 🎉
