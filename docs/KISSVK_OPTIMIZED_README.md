# 🎵 KissVK Music Module - Оптимизированная версия

## ✅ Что сделано

Модуль музыки полностью оптимизирован и очищен от устаревшего кода. Оставлен только минимально необходимый функционал для работы с библиотекой kissvk.

## 📁 Структура

### Основные файлы

```
backend/src/
├── services/
│   └── kissvk.service.js          # Единый оптимизированный сервис
├── controllers/
│   └── kissvk.controller.js       # Минимальный контроллер
├── modules/music/
│   ├── index.js                   # Упрощенная инициализация модуля
│   └── kissvk.routes.js           # Чистые роуты
└── utils/
    └── vk-audio-decoder-v3.js     # Утилита для расшифровки
```

## 🎯 Возможности

### KissVK Service (kissvk.service.js)

**Основные функции:**
- ✅ HTTP-only парсинг (без Puppeteer - низкая нагрузка на CPU)
- ✅ Расшифровка VK Audio URL
- ✅ Кеширование с TTL (1 час)
- ✅ Rate limiting (защита от перегрузки)
- ✅ Очередь запросов

**Методы:**
- `extractTracks(url, limit)` - Извлечь треки из HTML
- `decryptTracks(tracks)` - Расшифровать URL треков
- `getHomeTracks(limit)` - Треки с главной страницы
- `getNewTracks(limit)` - Новые треки
- `getChartTracks(limit)` - Топ чарт
- `searchTracks(query, limit)` - Поиск треков
- `getNewAlbums(limit)` - Новые альбомы
- `getChartAlbums(limit)` - Топ альбомов
- `getStats()` - Статистика сервиса
- `cleanCache()` - Очистка кеша

## 📡 API Endpoints

### GET /api/kissvk/preview
Превью треков без импорта в БД

**Query параметры:**
- `url` - URL страницы kissvk (по умолчанию `/`)
- `limit` - Количество треков (по умолчанию `20`)

**Пример:**
```bash
GET http://localhost:3000/api/kissvk/preview?url=/music/chart&limit=50
```

**Ответ:**
```json
{
  "success": true,
  "preview": true,
  "url": "https://kissvk.top/music/chart",
  "total": 50,
  "count": 50,
  "tracks": [
    {
      "trackId": "123456",
      "title": "Track Name",
      "artist": "Artist Name",
      "duration": 180,
      "streamUrl": "https://...",
      "coverUrl": "https://...",
      "source": "kissvk.top",
      "isDecrypted": true
    }
  ]
}
```

### POST /api/kissvk/import
Импорт треков в базу данных

**Body:**
```json
{
  "url": "/music/chart",
  "limit": 50,
  "createAlbum": true,
  "albumTitle": "Top Chart 2025",
  "albumArtist": "Various Artists"
}
```

**Ответ:**
```json
{
  "success": true,
  "imported": 45,
  "skipped": 5,
  "album": {
    "id": 123,
    "title": "Top Chart 2025",
    "artist": "Various Artists"
  },
  "tracks": [...],
  "skippedTracks": [...]
}
```

### GET /api/kissvk/search
Поиск треков

**Query параметры:**
- `q` - Поисковый запрос
- `limit` - Количество результатов (по умолчанию `20`)

**Пример:**
```bash
GET http://localhost:3000/api/kissvk/search?q=Скриптонит&limit=10
```

### GET /api/kissvk/albums/new
Получить новые альбомы

**Query параметры:**
- `limit` - Количество альбомов (по умолчанию `20`)

### GET /api/kissvk/albums/chart
Получить топ альбомов

**Query параметры:**
- `limit` - Количество альбомов (по умолчанию `50`)

### GET /api/kissvk/stats
Статистика сервиса

**Ответ:**
```json
{
  "success": true,
  "stats": {
    "requests": 150,
    "cacheHits": 45,
    "cacheMisses": 105,
    "errors": 2,
    "cacheSize": 89,
    "queueLength": 0,
    "activeRequests": 0,
    "cacheHitRate": 0.3
  }
}
```

### POST /api/kissvk/cache/clear
Очистить кеш

## 🔧 Технические детали

### Кеширование
- TTL: 1 час
- Автоматическая очистка каждые 10 минут
- Кеш ключ: `trackId:encryptedUrl`

### Rate Limiting
- Задержка между запросами: 1 секунда
- Максимум одновременных запросов: 2
- Очередь запросов

### Расшифровка URL
- Использует `vk-audio-decoder-v3`
- Пароль: `kissvk.top`
- Алгоритм: AES-256-CBC

## 🚀 Быстрый старт

### 1. Превью треков
```bash
curl "http://localhost:3000/api/kissvk/preview?limit=5"
```

### 2. Поиск
```bash
curl "http://localhost:3000/api/kissvk/search?q=pop&limit=10"
```

### 3. Импорт
```bash
curl -X POST http://localhost:3000/api/kissvk/import \
  -H "Content-Type: application/json" \
  -d '{
    "url": "/music/chart",
    "limit": 20,
    "createAlbum": true,
    "albumTitle": "Top 20"
  }'
```

### 4. Статистика
```bash
curl "http://localhost:3000/api/kissvk/stats"
```

## 📊 Преимущества новой версии

### ✅ Производительность
- **Без Puppeteer** - нет нагрузки на CPU от браузеров
- **HTTP-only** - быстрые и легкие запросы
- **Кеширование** - меньше запросов к kissvk.top

### ✅ Простота
- **Один сервис** вместо 3-х разных
- **Один контроллер** с понятными методами
- **Чистый код** без устаревшего функционала

### ✅ Надежность
- **Rate limiting** защищает от бана
- **Очередь запросов** предотвращает перегрузку
- **Обработка ошибок** на всех уровнях

### ✅ Расширяемость
- Легко добавить новые методы
- Понятная структура кода
- Singleton pattern для сервиса

## 🗑️ Удаленные файлы

Следующие файлы можно безопасно удалить (устарели):

```
backend/src/services/
├── kissvk-lightweight.service.js   # Объединен в kissvk.service.js
└── kissvk-puppeteer.service.js     # Не используется (Puppeteer убран)

backend/src/controllers/
├── kissvk-import.controller.js     # Заменен на kissvk.controller.js
└── kissvk-stream-proxy.controller.js  # Функционал не нужен

backend/src/routes/
├── kissvk-import.routes.js         # Заменен на kissvk.routes.js
└── kissvk-lightweight.routes.js    # Объединен

backend/src/schedulers/
├── lightweight-music-import.scheduler.js  # Не используется
├── music-auto-import.scheduler.js         # Не используется
└── kissvk-auto.scheduler.js               # Не используется
```

## 📝 Примечания

- Модуль полностью готов к работе
- Все зависимости минимальны: `axios`, `cheerio`, `crypto`
- Нет зависимости от `puppeteer` (большой размер и CPU)
- Код чистый, компактный и легко поддерживается

## 🔒 Безопасность

- Rate limiting защищает kissvk.top от перегрузки
- Кеширование уменьшает количество запросов
- Валидация входных данных
- Обработка ошибок

---

**Версия:** 2.0 (Оптимизированная)  
**Дата:** 2025-12-22  
**Статус:** ✅ Готово к продакшену
