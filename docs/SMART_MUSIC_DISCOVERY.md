# 🧠 Smart Music Discovery System

Интеллектуальная система импорта и подбора музыки с поддержкой:
- 🌍 **Импорт из iTunes RSS** (мировые чарты)
- 🇷🇺 **Импорт из Яндекс.Музыка** (российские чарты)
- 🎸 **Фильтрация по жанрам** (pop, rock, rap, electronic, indie и др.)
- 🌎 **Фильтрация по странам** (US, GB, RU, DE и др.)
- 💿 **Импорт новых альбомов** популярных исполнителей
- 🎯 **Персональные плейлисты** на основе лайков пользователей

---

## 📦 Установка

Все зависимости уже установлены. Система интегрирована в `server.js` и запускается автоматически.

---

## 🚀 Использование

### 1️⃣ Ручной запуск импорта

#### Умный импорт (все страны + жанры + альбомы):
```bash
docker exec errorparty_backend node /app/smart-import-music.js
```

**Что импортируется:**
- 🇺🇸 США: топ-50
- 🇬🇧 Великобритания: топ-30
- 🇷🇺 Россия: топ-50 (Яндекс.Музыка)
- 🇩🇪 Германия: топ-20
- 🎸 Жанры: pop, rock, rap, electronic, indie (по 30 треков)
- 💿 Новые альбомы популярных исполнителей

---

### 2️⃣ Создание персональных плейлистов

#### Для всех пользователей с лайками:
```bash
docker exec errorparty_backend node /app/create-personal-playlists.js
```

#### Для конкретного пользователя через API:
```bash
POST /api/music/discover/personal-playlist
Authorization: Bearer <token>
```

**Ответ:**
```json
{
  "success": true,
  "playlist": {
    "playlistId": 123,
    "playlistName": "Мои рекомендации 02.12.2025",
    "tracksCount": 30,
    "topGenres": ["Pop", "Rock", "Electronic"],
    "topArtists": ["Taylor Swift", "Imagine Dragons", "Daft Punk"]
  }
}
```

---

## ⚙️ Автоматический импорт

Система настроена на автоматический запуск:

| Время | Задача | Описание |
|-------|--------|----------|
| **3:00** | Топ-хиты iTunes | Базовый импорт мировых чартов |
| **4:00** | Умный импорт | Страны + жанры + альбомы |
| **5:00** | Персональные плейлисты | Для всех пользователей с лайками |

---

## 🎯 API Endpoints

### Для пользователей:

#### Создать персональный плейлист
```http
POST /api/music/discover/personal-playlist
Authorization: Bearer <token>
```

#### Получить рекомендации (без создания плейлиста)
```http
GET /api/music/discover/recommendations?limit=20
Authorization: Bearer <token>
```

**Ответ:**
```json
{
  "success": true,
  "recommendations": [...],
  "basedOnGenres": ["Pop", "Rock"],
  "basedOnArtists": ["Artist 1", "Artist 2"]
}
```

---

### Для администраторов:

#### Импорт топа по стране
```http
POST /api/music/discover/import-country
Authorization: Bearer <admin-token>
Content-Type: application/json

{
  "country": "us",
  "limit": 50,
  "genres": ["Pop", "Rock"]  // опционально
}
```

#### Импорт топа по жанру
```http
POST /api/music/discover/import-genre
Authorization: Bearer <admin-token>
Content-Type: application/json

{
  "genre": "rock",
  "limit": 30
}
```

#### Импорт новых альбомов
```http
POST /api/music/discover/import-albums
Authorization: Bearer <admin-token>
```

---

## 🎵 Поддерживаемые страны

| Код | Страна | Источник |
|-----|--------|----------|
| `us` | США | iTunes RSS |
| `gb` | Великобритания | iTunes RSS |
| `de` | Германия | iTunes RSS |
| `fr` | Франция | iTunes RSS |
| `ru` | Россия | Яндекс.Музыка |
| `jp` | Япония | iTunes RSS |
| `kr` | Южная Корея | iTunes RSS |

---

## 🎸 Поддерживаемые жанры

- `pop` - Поп-музыка
- `rock` - Рок
- `rap` - Рэп / Хип-хоп
- `electronic` - Электронная музыка
- `dance` - Танцевальная
- `indie` - Инди
- `metal` - Метал
- `jazz` - Джаз
- `classical` - Классика
- `alternative` - Альтернатива

---

## 📊 Как работают персональные плейлисты

1. **Анализ лайков**: Система анализирует все треки, которые пользователь лайкнул
2. **Определение топ-жанров**: Выявляются 3 самых популярных жанра
3. **Определение топ-исполнителей**: Выявляются 5 любимых исполнителей
4. **Подбор треков**: Ищутся похожие треки, которые пользователь еще не лайкал
5. **Создание плейлиста**: Формируется плейлист из 30 рекомендованных треков

**Пример:**
- Пользователь лайкнул: Taylor Swift (5 треков), Imagine Dragons (3 трека)
- Топ-жанры: Pop, Rock, Alternative
- Рекомендации: Треки похожих исполнителей и жанров

---

## 🔧 Настройка

### Изменить расписание импорта

Отредактируйте `backend/setup-smart-import.js`:

```javascript
// Умный импорт в 4:00
cron.schedule('0 4 * * *', () => { ... });

// Персональные плейлисты в 5:00
cron.schedule('0 5 * * *', () => { ... });
```

### Изменить страны для импорта

Отредактируйте `backend/smart-import-music.js`:

```javascript
const countries = [
  { code: 'us', name: 'США', limit: 50 },
  { code: 'gb', name: 'Великобритания', limit: 30 },
  { code: 'ru', name: 'Россия', limit: 50 },
  { code: 'jp', name: 'Япония', limit: 20 }  // Добавить новую
];
```

### Изменить жанры для импорта

```javascript
const genres = ['pop', 'rock', 'rap', 'electronic', 'indie', 'metal'];
```

---

## 🐛 Отладка

### Проверить работу Яндекс.Музыка:
```bash
docker exec errorparty_backend node -e "
  const yandex = require('./src/services/yandex-music.service');
  yandex.getRussianTop100().then(tracks => {
    console.log('Получено треков:', tracks.length);
    tracks.slice(0, 5).forEach((t, i) => {
      console.log(\`\${i+1}. \${t.artist} - \${t.title}\`);
    });
  }).then(() => process.exit(0));
"
```

### Проверить персональные рекомендации:
```bash
docker exec errorparty_backend node -e "
  const smart = require('./src/services/smart-discovery.service');
  smart.createPersonalPlaylist(1).then(result => {
    console.log('Плейлист:', result);
  }).then(() => process.exit(0));
"
```

---

## 📝 Логи

Все операции импорта логируются:
- ✅ Успешные импорты
- ❌ Ошибки парсинга
- 📊 Статистика по странам/жанрам
- 🎵 Количество найденных/не найденных треков

---

## 🎉 Готово!

Система автоматически импортирует музыку каждый день и создает персональные плейлисты для пользователей.

**Проверьте результат:**
- Админка: `errorparty.ru/admin/music`
- Пользовательские плейлисты: `errorparty.ru/music/playlists`
- API: `errorparty.ru/api/music/discover/recommendations`
