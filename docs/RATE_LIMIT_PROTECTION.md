# 🛡️ Защита от Steam Rate Limit

## Что изменилось?

Добавлена умная система защиты от блокировки Steam аккаунта при частых перезапусках.

## Как это работает?

### 1. Экспоненциальная задержка
При ошибках подключения бот увеличивает время ожидания:
- 1-я попытка: 1 минута
- 2-я попытка: 2 минуты
- 3-я попытка: 4 минуты
- 4-я попытка: 8 минут
- ...и так далее до максимум 30 минут

### 2. Обнаружение Rate Limit
Когда Steam возвращает ошибку `RateLimitExceeded` (код 84):
- ✅ Бот автоматически останавливает попытки входа
- ⏰ Ждёт 1 час перед следующей попыткой
- 📊 Сбрасывает счётчик попыток
- 🔔 Логирует понятное сообщение

### 3. Сброс при успехе
При успешном подключении все счётчики сбрасываются:
```javascript
this.loginAttempts = 0;
this.isRateLimited = false;
```

## Логи

### При Rate Limit:
```
❌ Steam Client Error: RateLimitExceeded
🚫 Rate limit detected! Waiting 60 minutes before retry...
⏰ Bot will automatically reconnect after cooldown
```

### При обычных ошибках:
```
❌ Steam Client Error: LogonSessionReplaced
⏱️  Reconnecting in 60 seconds (attempt 1)...
```

### При отключении:
```
⚠️  Steam Bot disconnected: NoConnection
⏸️  Skipping reconnect - rate limited
```

## Статус бота

Теперь `getStatus()` возвращает дополнительные поля:
```json
{
  "connected": false,
  "gcReady": false,
  "rateLimited": true,
  "loginAttempts": 3,
  "friends": 4
}
```

## Ручной сброс (для админов)

Если нужно сбросить rate limit вручную:
```javascript
const bot = getSteamBot();
bot.resetRateLimit();
```

Или через API (добавить эндпоинт в cs2Controller):
```bash
curl -X POST https://errorparty.ru/api/cs2/bot/reset-limit \
  -H "Authorization: Bearer ADMIN_TOKEN"
```

## Рекомендации

### ❌ Не делать:
- Частые перезапуски контейнера (<5 минут)
- Множественные rebuilds без необходимости
- Удаление volume `steam_sentry` при каждом rebuild

### ✅ Делать:
- Использовать `docker-compose restart backend` вместо rebuild
- Сохранять volume `steam_sentry` с credentials
- Ждать 1 час после rate limit перед новым запуском
- При разработке использовать отдельный тестовый Steam аккаунт

## Текущая ситуация

**Статус:** 🚫 Rate Limited до ~04:15 UTC (23 ноября 2025)

**Что произошло:**
- Множественные пересборки контейнера
- Каждый rebuild = новая попытка входа
- Steam заблокировал аккаунт на 1 час

**Что делать:**
1. ✅ **Дождаться снятия блокировки** (1 час с последней попытки)
2. ✅ **Не перезапускать backend** до истечения времени
3. ✅ **После разблокировки:**
   ```bash
   docker-compose up -d backend
   ```
4. ✅ **Проверить логи:**
   ```bash
   docker logs errorparty_backend | grep "Steam Bot logged in"
   ```

## Production Ready

Система теперь безопасна для production:
- ✅ Автоматическое восстановление после ошибок
- ✅ Защита от блокировки аккаунта
- ✅ Понятные логи для диагностики
- ✅ Graceful handling rate limits
- ✅ Persistent credentials через volume

## Мониторинг

Добавьте алерт на rate limit:
```javascript
steamBot.on('error', (err) => {
  if (err.eresult === 84) {
    // Отправить уведомление админу
    notifyAdmin('Steam bot rate limited!');
  }
});
```

---

**Важно:** После снятия rate limit бот автоматически подключится. Никаких действий не требуется!
