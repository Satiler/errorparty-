# 🎨 Руководство по фронтенду GSI

## Обзор изменений

Фронтенд был полностью переработан для поддержки Game State Integration (GSI) системы отслеживания матчей CS2.

## 🆕 Новые компоненты

### 1. GSIStatus Component
**Расположение:** `frontend/src/components/GSIStatus.jsx`

**Назначение:** Отображает текущий статус GSI системы и количество активных матчей.

**Особенности:**
- ✅ Автообновление каждые 30 секунд
- ✅ Индикатор активности (зеленый пульсирующий = активен)
- ✅ Показывает количество активных матчей
- ✅ Время последнего обновления

**Пример использования:**
```jsx
import GSIStatus from '../components/GSIStatus';

// В компоненте:
<GSIStatus />
```

## 📊 Обновленные страницы

### CS2StatsPage.jsx

**Новые функции:**

#### 1. Баннер живых матчей
Отображается автоматически когда пользователь играет в CS2 с установленным GSI конфигом.

```jsx
{/* Live GSI Matches Banner */}
{liveMatches.length > 0 && (
  <motion.div className="mb-6 bg-gradient-to-r from-green-900/50...">
    {/* Отображение текущей статистики в реальном времени */}
  </motion.div>
)}
```

**Показывает:**
- 🗺️ Карта
- 🎯 K/D/A (обновляется в реальном времени)
- 🏆 Счет раундов
- 👥 Команда (CT/T)

#### 2. Баннер настройки GSI
Показывается владельцу профиля если нет активных матчей.

```jsx
{isOwner && liveMatches.length === 0 && (
  <div className="mb-6 bg-gradient-to-r from-blue-900/50...">
    {/* Ссылка на инструкцию по установке GSI */}
  </div>
)}
```

#### 3. Индикаторы источника данных

**В таблице матчей:**
- 🟢 **GSI** - зеленая метка для матчей из Game State Integration
- 🔵 **SC** - синяя метка для матчей добавленных по Share Code

```jsx
// Колонка "Источник" в таблице
<td className="p-3">
  {isGSI ? (
    <div className="flex items-center gap-2">
      <div className="w-3 h-3 bg-green-500 rounded-full"></div>
      <span className="text-green-400 font-bold">GSI</span>
    </div>
  ) : (
    <div className="flex items-center gap-2">
      <div className="w-3 h-3 bg-blue-500 rounded-full"></div>
      <span className="text-blue-400 font-bold">SC</span>
    </div>
  )}
</td>
```

#### 4. Информационные блоки

**GSI Info Block:**
```markdown
✅ Game State Integration (GSI)
- Автоматическое отслеживание в реальном времени
- Полная статистика (K/D/A, карта, раунды, MVP)
- Нет зависимости от демо-файлов
- Работает во всех режимах игры
```

**Share Code Info Block:**
```markdown
📝 Матчи, добавленные по Share Code
- Показывают только базовую информацию (Match ID)
- ⚠️ Без детальной статистики
- 💡 Используйте GSI для автоматического получения полной статистики
```

### ProfilePage.jsx

**Обновления:**

#### CS2 Stats Card
Добавлен индикатор готовности GSI:

```jsx
<span className="px-2 py-1 bg-green-500/20 text-green-400 rounded text-xs">
  <div className="w-2 h-2 bg-green-500 rounded-full"></div>
  GSI Ready
</span>
<span className="text-gray-500 text-xs">Автоматическое отслеживание</span>
```

## 🔄 Логика обновления данных

### Интервалы обновления

| Компонент | Интервал | Назначение |
|-----------|----------|------------|
| **Live Matches** | 10 секунд | Обновление текущей статистики матча |
| **GSI Status** | 30 секунд | Проверка статуса GSI системы |
| **Match History** | По действию | Загрузка после завершения матча |

### Жизненный цикл матча

```
1. Игрок запускает CS2 с GSI конфигом
   ↓
2. Фронтенд получает данные через /api/gsi/active
   ↓
3. Отображается баннер "Сейчас в игре"
   ↓
4. Статистика обновляется каждые 10 секунд
   ↓
5. При завершении матча данные сохраняются в БД
   ↓
6. Матч появляется в истории с меткой 🟢 GSI
```

## 🎨 Стилизация

### Цветовая схема

| Элемент | Цвет | Класс |
|---------|------|-------|
| GSI метка | Зеленый | `bg-green-500/20 text-green-400` |
| Share Code метка | Синий | `bg-blue-500/20 text-blue-400` |
| Живой матч фон | Изумрудный | `from-green-900/50 to-emerald-900/50` |
| GSI настройка фон | Голубой | `from-blue-900/50 to-cyan-900/50` |
| Активный индикатор | Зеленый пульсирующий | `animate-pulse` + `animate-ping` |

### Анимации

**Появление живого матча:**
```jsx
<motion.div
  initial={{ opacity: 0, y: -10 }}
  animate={{ opacity: 1, y: 0 }}
>
```

**Пульсирующий индикатор:**
```jsx
<div className="w-4 h-4 bg-green-500 rounded-full animate-pulse"></div>
<div className="absolute inset-0 w-4 h-4 bg-green-500 rounded-full animate-ping"></div>
```

## 📱 Адаптивность

Все новые компоненты полностью адаптивны:

- **Desktop:** Полная информация в строках
- **Tablet:** 2 колонки в гриде живых матчей
- **Mobile:** 1 колонка, вертикальная компоновка

```jsx
<div className="grid grid-cols-2 md:grid-cols-4 gap-4">
  {/* Адаптивный грид */}
</div>
```

## 🔧 API Эндпоинты

### Используемые эндпоинты

1. **GET /api/gsi/active**
   - Возвращает активные GSI матчи
   - Используется для баннера живых матчей
   - Обновление: каждые 10 секунд

2. **GET /api/cs2/matches/:steamId**
   - Возвращает историю матчей
   - Включает поле `source` ('gsi' или 'share_code')
   - Используется для таблицы истории

3. **GET /api/cs2/stats/:steamId**
   - Возвращает общую статистику
   - Работает с обоими источниками данных

## 🐛 Отладка

### Console Logs

```javascript
// При загрузке живых матчей
console.log('Live matches:', liveMatches);

// При ошибке GSI Status
console.error('GSI status check failed:', err);

// При ошибке загрузки матчей
console.error('Failed to fetch live matches:', err);
```

### Проверка состояния

**В React DevTools:**
```javascript
// CS2StatsPage state
{
  liveMatches: [], // Активные матчи
  loadingLiveMatches: false, // Загрузка
  matches: [] // История матчей (с source: 'gsi' или 'share_code')
}

// GSIStatus state
{
  status: {
    isActive: true, // GSI активен?
    lastUpdate: Date, // Время последнего обновления
    activeMatches: 1 // Количество активных
  }
}
```

## 📈 Производительность

### Оптимизации

1. **Кэширование:** 
   - Axios автоматически кэширует GET запросы
   - `useMemo` для фильтрации матчей по дате

2. **Ленивая загрузка:**
   - Компоненты загружаются по требованию
   - Модалы появляются только при клике

3. **Debouncing:**
   - Интервалы с `clearInterval` при размонтировании
   - Предотвращение утечек памяти

## 🚀 Будущие улучшения

### Планируется добавить:

- [ ] **WebSocket для real-time обновлений**
  - Мгновенное обновление статистики без polling
  - Уведомления о начале/окончании матча

- [ ] **Детализированная live-статистика**
  - График K/D по раундам
  - Карта убийств на миникарте
  - Экономика по раундам

- [ ] **Исторические графики**
  - Прогресс winrate за период
  - Тренд K/D ratio
  - Активность по часам/дням

- [ ] **Сравнение матчей**
  - Выбор нескольких матчей для сравнения
  - Side-by-side статистика
  - Выявление улучшений/ухудшений

## 📚 Дополнительные материалы

- [GSI Setup Guide](./GSI_SETUP_GUIDE.md) - Установка для пользователей
- [GSI Migration](./GSI_MIGRATION.md) - Техническая миграция
- [API Documentation](./API.md) - Полная документация API

## ❓ FAQ

**Q: Почему мои старые матчи показываются с синей меткой SC?**  
A: Это матчи добавленные через Share Code до установки GSI. Они содержат только базовую информацию.

**Q: Как обновить матч со SC на GSI?**  
A: Переиграйте этот матч с установленным GSI конфигом. Новый матч будет сохранен с полной статистикой.

**Q: Баннер живого матча не появляется, что делать?**  
A: 
1. Проверьте установлен ли `gamestate_integration_errorparty.cfg`
2. Убедитесь что CS2 запущен
3. Проверьте `/api/gsi/active` в браузере
4. Смотрите логи backend: `docker logs errorparty_backend --tail 50`

**Q: Можно ли полностью отключить Share Code систему?**  
A: Нет, она остается для ручного добавления конкретных матчей. Но GSI автоматически захватывает все матчи.

---

**Обновлено:** 23 ноября 2025  
**Версия:** 2.0.0 (GSI Release)
