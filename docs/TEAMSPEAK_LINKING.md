# 🔗 Система связывания Steam и TeamSpeak

## 📋 Описание

Реализована автоматическая система связывания аккаунтов Steam и TeamSpeak через сайт с использованием токенов.

## 🎯 Как это работает

### Для пользователя:

1. **Вход на сайт** - пользователь авторизуется через Steam
2. **Переход в настройки** - клик на иконку ⚙️ в навбаре или `/settings`
3. **Генерация токена** - нажатие кнопки "Связать TeamSpeak"
4. **Получение токена** - система генерирует уникальный 6-значный код (например, `ABC123`)
5. **Вход в TeamSpeak** - подключение к серверу `errorparty.ru`
6. **Ввод команды** - в чате TeamSpeak: `!link ABC123`
7. **Автоматическая связка** - бот мгновенно связывает аккаунты
8. **Подтверждение** - пользователь получает сообщение в TS и видит обновление на сайте

### Технический процесс:

```
Пользователь                  Сайт                    TeamSpeak Бот
    |                          |                            |
    |--[Нажимает "Связать"]-->|                            |
    |                          |                            |
    |<--[Токен: ABC123]--------|                            |
    |                          |                            |
    |----------[Зайти в TS]---------------------------->|
    |                          |                            |
    |----------[!link ABC123]-------------------------->|
    |                          |                            |
    |                          |<--[Проверка токена]--------|
    |                          |                            |
    |                          |--[Сохранить teamspeakUid]->|
    |                          |                            |
    |<--[✅ Связано!]----------|                            |
    |                          |                            |
    |<---------[✅ Аккаунт связан, username!]--------------|
```

## 🗄️ Структура данных

### Новая модель: `LinkToken`

```javascript
{
  id: Integer,
  token: String(6),           // Уникальный код (ABC123)
  userId: Integer,            // ID пользователя на сайте
  teamspeakUid: String,       // UID клиента TS (заполняется после связки)
  isUsed: Boolean,            // Использован ли токен
  expiresAt: Date,            // Время истечения (+15 минут)
  createdAt: Date,
  updatedAt: Date
}
```

## 🔌 API Endpoints

### `POST /api/user/generate-link-token`
**Требует авторизации**

Генерирует новый токен для связывания.

**Response:**
```json
{
  "success": true,
  "token": "ABC123",
  "expiresAt": "2025-11-21T15:30:00.000Z",
  "message": "Токен сгенерирован. Используйте команду !link ABC123 в чате TeamSpeak"
}
```

### `GET /api/user/check-link-status`
**Требует авторизации**

Проверяет статус связывания.

**Response (связан):**
```json
{
  "success": true,
  "linked": true,
  "message": "TeamSpeak аккаунт успешно связан"
}
```

**Response (не связан):**
```json
{
  "success": true,
  "linked": false,
  "hasActiveToken": true,
  "token": "ABC123"
}
```

## 🤖 TeamSpeak команды

### `!link ТОКЕН`
Связывает аккаунт с сайтом.

**Пример:**
```
!link ABC123
```

**Ответ при успехе:**
```
✅ Аккаунт успешно связан! Добро пожаловать, username!
```

**Возможные ошибки:**
- `❌ Токен не найден или истёк. Попробуйте сгенерировать новый на сайте.`
- `❌ Неверный формат команды. Используйте: !link ТОКЕН`
- `❌ Этот аккаунт уже связан с другим TeamSpeak клиентом.`

### `!help` или `!commands`
Показывает список доступных команд.

## 🎨 Frontend

### Страница настроек: `/settings`

**Компоненты:**
- Карточка информации о профиле
- Карточка связывания TeamSpeak
- Инструкция с пошаговыми действиями
- Большой отображаемый токен с кнопкой копирования
- Автоматическая проверка статуса каждые 3 секунды
- Анимация загрузки при ожидании связывания

**Особенности:**
- Токен действителен 15 минут
- Старые токены автоматически удаляются при генерации нового
- Можно отвязать аккаунт (кнопка "Отвязать TeamSpeak")
- Мгновенное обновление UI при успешной связке

## 📦 Установка и запуск

### 1. Обновить базу данных

```bash
cd backend
node src/database/migrate.js
```

### 2. Перезапустить backend

```bash
npm run dev
```

### 3. Проверить TeamSpeak сервис

Убедитесь, что TeamSpeak ServerQuery подключен и слушает события `textmessage`.

## 🔧 Настройка

### Environment variables (.env)

```env
TS_HOST=localhost
TS_QUERY_PORT=10011
TS_SERVER_PORT=9987
TS_QUERY_USER=serveradmin
TS_QUERY_PASSWORD=your_password
TS_QUERY_NICKNAME=ErrorParty Bot
```

## 🧪 Тестирование

### Вручную:

1. Войти на сайт через Steam
2. Перейти в `/settings`
3. Нажать "Связать TeamSpeak"
4. Скопировать токен
5. Зайти в TeamSpeak
6. Написать `!link ТОКЕН`
7. Проверить, что на сайте появилось "✅ TeamSpeak успешно связан!"

### Проверка логов:

Backend будет показывать:
```
💬 Message from UserNickname: !link ABC123
🔗 Link request from UserNickname with token: ABC123
✅ Linked user username with TS client UserNickname (UID: unique_id)
```

## 🚀 Преимущества

✅ **Автоматизация** - не нужно вручную вводить Steam ID в TeamSpeak  
✅ **Безопасность** - токены истекают через 15 минут  
✅ **Простота** - всего одна команда в чате  
✅ **Мгновенность** - связка происходит за секунды  
✅ **UX** - понятная инструкция и визуальная обратная связь  
✅ **Отслеживание** - бот записывает, кто и когда связался  

## 📝 TODO (опционально)

- [ ] Email уведомление при успешной связке
- [ ] История связываний в профиле
- [ ] Rate limiting на генерацию токенов (1 токен в минуту)
- [ ] Webhook для отправки в Discord при новых связках
- [ ] Админ панель для просмотра всех связок

## 🐛 Troubleshooting

### Токен не работает
- Проверьте, что TeamSpeak бот подключен
- Убедитесь, что токен не истёк (15 минут)
- Проверьте правильность ввода команды (регистр не важен)

### Связка не отображается на сайте
- Проверьте консоль браузера на ошибки
- Убедитесь, что backend получил событие от TS
- Проверьте логи backend на наличие ошибок

### Бот не отвечает в TeamSpeak
- Проверьте подключение к ServerQuery
- Убедитесь, что бот слушает события `textmessage`
- Проверьте права бота в TeamSpeak
