# Система выбора квестов

## Обзор

Добавлена возможность для пользователей самостоятельно выбирать квесты из доступного пула вместо случайного назначения.

## Расширенный пул квестов

### Dota 2
- **Easy**: ~40 квестов различной направленности
- **Medium**: ~30 квестов для игроков уровня 5+
- **Hard**: ~25 квестов для игроков уровня 10+
- **Epic**: 5 квестов для игроков уровня 15+

### CS2
- **Easy**: ~22 квеста на базовые навыки
- **Medium**: ~20 квестов на среднюю сложность (уровень 5+)
- **Hard**: ~15 квестов для опытных игроков (уровень 10+)
- **Epic**: 9 квестов для элитных игроков (уровень 15+)

## API Endpoints

### 1. Получить доступные квесты
```http
GET /api/quests/available?game=dota2&type=daily
Authorization: Bearer <token>
```

**Query параметры:**
- `game` - `dota2` или `cs2`
- `type` - `daily` или `weekly`

**Response:**
```json
{
  "success": true,
  "userLevel": 3,
  "availableDifficulties": ["easy", "medium"],
  "questsByDifficulty": {
    "easy": [
      {
        "id": 1,
        "key": "daily_win",
        "title": "🎖️ Победитель",
        "description": "Выиграй 1 матч",
        "game": "dota2",
        "type": "daily",
        "difficulty": "easy",
        "requirement": { "type": "win", "value": 1 },
        "reward": { "xp": 50, "coins": 10 },
        "icon": "🎖️",
        "isAssigned": false
      },
      ...
    ],
    "medium": [...],
    "hard": [],
    "epic": []
  },
  "maxQuests": 3,
  "currentlyAssigned": 0
}
```

### 2. Выбрать квесты
```http
POST /api/quests/select
Authorization: Bearer <token>
Content-Type: application/json

{
  "questIds": [1, 5, 12],
  "type": "daily"
}
```

**Body параметры:**
- `questIds` - массив ID выбранных квестов
- `type` - `daily` (максимум 3 квеста) или `weekly` (максимум 2 квеста)

**Response:**
```json
{
  "success": true,
  "assigned": 3,
  "quests": [
    {
      "id": 123,
      "questId": 1,
      "progress": 0,
      "targetValue": 1,
      "expiresAt": "2025-01-22T23:59:59.999Z"
    },
    ...
  ],
  "message": "Успешно выбрано 3 ежедневных заданий"
}
```

## Логика работы

### Доступность квестов по уровням
- **Уровень 1-4**: только Easy квесты
- **Уровень 5-9**: Easy + Medium квесты
- **Уровень 10-14**: Easy + Medium + Hard квесты
- **Уровень 15+**: Все квесты включая Epic

### Ограничения
- **Daily квесты**: максимум 3 активных одновременно
- **Weekly квесты**: максимум 2 активных одновременно
- Квесты можно выбирать только из доступных для текущего уровня
- При выборе новых квестов старые удаляются

### Примеры использования

#### Frontend: Получение доступных квестов
```javascript
async function getAvailableQuests(game, type) {
  const response = await fetch(
    `/api/quests/available?game=${game}&type=${type}`,
    {
      headers: {
        'Authorization': `Bearer ${token}`
      }
    }
  );
  return await response.json();
}
```

#### Frontend: Выбор квестов пользователем
```javascript
async function selectQuests(questIds, type) {
  const response = await fetch('/api/quests/select', {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${token}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({ questIds, type })
  });
  return await response.json();
}

// Пример использования
const selectedIds = [1, 5, 12]; // IDs выбранных квестов
const result = await selectQuests(selectedIds, 'daily');
console.log(result.message); // "Успешно выбрано 3 ежедневных заданий"
```

## Новые функции в questService

### getAvailableQuests(userId, game, type)
Получает все доступные квесты для пользователя с учетом его уровня.

**Параметры:**
- `userId` - ID пользователя
- `game` - 'dota2' или 'cs2'
- `type` - 'daily' или 'weekly'

**Возвращает:**
- Объект с группированными по сложности квестами
- Информацию об уровне пользователя
- Список доступных сложностей
- Количество уже назначенных квестов

### selectQuests(userId, questIds, type)
Назначает выбранные пользователем квесты.

**Параметры:**
- `userId` - ID пользователя
- `questIds` - массив ID квестов
- `type` - 'daily' или 'weekly'

**Возвращает:**
- Массив назначенных UserQuest объектов

**Выбрасывает ошибку если:**
- Превышен лимит квестов (3 для daily, 2 для weekly)
- Квест недоступен для уровня пользователя
- Квест не найден

## UI/UX рекомендации

### Отображение квестов
1. Группировать квесты по сложности
2. Показывать награды (XP, монеты, роли)
3. Отмечать уже назначенные квесты
4. Блокировать недоступные квесты (серый цвет + замок)

### Выбор квестов
1. Позволить выбрать до 3 daily / 2 weekly квестов
2. Показывать счетчик выбранных квестов (например, "2/3")
3. Кнопка "Подтвердить выбор" активна только при выборе правильного количества
4. После выбора показать уведомление об успехе

### Пример компонента
```jsx
function QuestSelector({ game, type }) {
  const [available, setAvailable] = useState(null);
  const [selected, setSelected] = useState([]);
  
  useEffect(() => {
    loadAvailableQuests();
  }, [game, type]);
  
  const maxQuests = type === 'daily' ? 3 : 2;
  
  const handleSelect = (questId) => {
    if (selected.includes(questId)) {
      setSelected(selected.filter(id => id !== questId));
    } else if (selected.length < maxQuests) {
      setSelected([...selected, questId]);
    }
  };
  
  const handleConfirm = async () => {
    const result = await selectQuests(selected, type);
    if (result.success) {
      alert(result.message);
      onQuestsSelected();
    }
  };
  
  return (
    <div>
      <h3>Выберите квесты ({selected.length}/{maxQuests})</h3>
      {available?.questsByDifficulty.map(([difficulty, quests]) => (
        <div key={difficulty}>
          <h4>{difficulty}</h4>
          {quests.map(quest => (
            <QuestCard
              key={quest.id}
              quest={quest}
              selected={selected.includes(quest.id)}
              disabled={quest.isAssigned}
              onSelect={() => handleSelect(quest.id)}
            />
          ))}
        </div>
      ))}
      <button 
        onClick={handleConfirm}
        disabled={selected.length !== maxQuests}
      >
        Подтвердить
      </button>
    </div>
  );
}
```

## Тестирование

Создан тестовый скрипт `test-quest-selection.js`:

```bash
# В Docker контейнере
docker exec errorparty_backend node test-quest-selection.js
```

Тест проверяет:
1. ✅ Получение доступных квестов для Dota 2
2. ✅ Группировку квестов по сложности
3. ✅ Выбор 3 случайных easy квестов
4. ✅ Назначение выбранных квестов
5. ✅ Проверку назначенных квестов
6. ✅ Получение доступных квестов для CS2

## Миграция с автоматического назначения

### Старый способ (автоматическое назначение)
```javascript
// Назначает 3 случайных квеста автоматически
await questService.assignQuests(userId, 'dota2', 'daily');
```

### Новый способ (выбор пользователем)
```javascript
// 1. Получить доступные квесты
const available = await questService.getAvailableQuests(userId, 'dota2', 'daily');

// 2. Пользователь выбирает из UI
const selectedIds = [1, 5, 12];

// 3. Назначить выбранные
await questService.selectQuests(userId, selectedIds, 'daily');
```

**Примечание:** Старый метод `assignQuests` остается доступным для CRON-задач и автоматического назначения.

## Совместимость

- ✅ Старые квесты продолжают работать
- ✅ Автоматическое назначение через CRON не затронуто
- ✅ Обратная совместимость с существующими UserQuest записями
- ✅ Все существующие эндпоинты работают без изменений

## Будущие улучшения

1. **Фильтры квестов**: поиск по типу задания (kills, assists, damage и т.д.)
2. **Избранные квесты**: возможность добавить квесты в избранное
3. **История выбора**: показывать какие квесты пользователь выбирал чаще всего
4. **Рекомендации**: предлагать квесты на основе стиля игры пользователя
5. **Обмен квестов**: возможность поменять 1 квест в день без потери прогресса
