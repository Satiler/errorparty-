# 🌊 Моя волна - Персональный музыкальный поток

## Описание

"Моя волна" - это бесконечный персонализированный поток музыки, подобранный на основе ваших предпочтений. Аналог функционала Яндекс.Музыки.

## Как это работает

### Алгоритм рекомендаций

Волна генерируется с балансировкой следующих категорий:

- **30% - Знакомые треки** 
  - От ваших любимых исполнителей
  - Треки, которые вы ещё не слышали

- **40% - Похожие треки**
  - По жанрам из вашей истории
  - От новых исполнителей в знакомых жанрах
  - Сортировка по популярности

- **20% - Открытия**
  - Новые исполнители
  - Схожие временные периоды (десятилетия)
  - Расширение музыкального горизонта

- **10% - Популярное**
  - Тренды
  - Хиты с высоким playCount
  - Новые релизы

### Анализ предпочтений

Система анализирует:

1. **История прослушиваний** (90 дней, вес x1)
   - Жанры
   - Исполнители
   - Временные периоды
   - Время суток

2. **Лайки** (вес x3)
   - Имеют больший приоритет
   - Указывают на явные предпочтения

3. **Умное перемешивание**
   - Избегает повторов одного исполнителя подряд
   - Разнообразие в потоке

## Backend API

### Endpoint

```
GET /api/music/my-wave
```

**Требуется авторизация:** ✅ Да (Bearer token)

### Параметры запроса

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `size` | number | 20 | Количество треков в порции |
| `exclude` | string | "" | Comma-separated список ID уже показанных треков |

### Пример запроса

```bash
curl -H "Authorization: Bearer YOUR_TOKEN" \
  "https://errorparty.ru/api/music/my-wave?size=20&exclude=1,2,3"
```

### Ответ

```json
{
  "success": true,
  "tracks": [
    {
      "id": 123,
      "title": "Track Name",
      "artist": "Artist Name",
      "genre": "Rock",
      "year": 2020,
      // ... другие поля трека
    }
  ],
  "metadata": {
    "familiar": 6,
    "similar": 8,
    "discovery": 4,
    "trending": 2
  }
}
```

### Fallback режим

Если у пользователя недостаточно данных (< 5 прослушиваний И < 3 лайка):

```json
{
  "success": true,
  "tracks": [ /* популярные треки */ ],
  "metadata": {
    "fallback": true
  }
}
```

## Frontend

### Маршрут

```
/music/wave
```

### Основные функции

1. **Бесконечная прокрутка**
   - `IntersectionObserver` отслеживает конец списка
   - Автоматическая подгрузка при приближении к низу
   - Порог: `rootMargin: '100px'`

2. **Управление состоянием**
   - `loadedTrackIds` - Set для отслеживания уже показанных треков
   - `hasMore` - флаг наличия следующих порций
   - `loadingMore` - индикатор загрузки

3. **Статистика**
   - Отображение соотношения категорий
   - Визуальные индикаторы (цвета)
   - Подсказки для пользователя

### Компонент

```jsx
<MyWave />
```

**Используемые компоненты:**
- `TrackRow` - строка трека
- `LoadingSpinner` - индикатор загрузки

## Архитектура

### Backend (Node.js)

```
backend/src/modules/music/
├── my-wave.service.js      # Сервис генерации волны
├── music.controller.js     # Контроллер (getMyWave)
└── music.routes.js         # Роутинг
```

### Frontend (React)

```
frontend/src/
├── pages/MyWave.jsx        # Страница волны
├── pages/MusicHomePage.jsx # Карточка на главной
└── App.jsx                 # Роут /music/wave
```

## Использование

### Для пользователя

1. Перейдите на `/music/wave` или нажмите "Моя волна" на главной
2. Прокручивайте список вниз - треки подгружаются автоматически
3. Лайкайте треки ❤️ - алгоритм становится точнее
4. Нажмите "Обновить" для новой волны

### Для разработчика

**Добавить новый фактор в алгоритм:**

```javascript
// backend/src/modules/music/my-wave.service.js

async _getUserProfile(userId) {
  // Добавить новый анализ
  const newFactor = await this._analyzeNewFactor(userId);
  
  return {
    ...profile,
    newFactor
  };
}
```

**Изменить балансировку:**

```javascript
// В методе generateWave()
const familiarTracks = await this._getFamiliarTracks(
  profile, 
  Math.floor(size * 0.4)  // Было 0.3, стало 0.4
);
```

## Улучшения (TODO)

### v1.1 - Обратная связь

- [ ] Таблица `TrackSkips` для пропущенных треков
- [ ] Учёт пропусков в алгоритме (снижение веса)
- [ ] Длительность прослушивания (влияние на вес)

### v1.2 - Контекстность

- [ ] Время суток (утро/день/вечер/ночь)
- [ ] День недели (будни/выходные)
- [ ] Настроение (mood tags)

### v1.3 - Социальность

- [ ] Анализ схожих пользователей
- [ ] "Что слушают похожие люди"
- [ ] Collaborative filtering

### v1.4 - ML модель

- [ ] TensorFlow.js для клиентской стороны
- [ ] Feature extraction (темп, энергия, танцевальность)
- [ ] Нейросеть для предсказания

## Производительность

### Оптимизации

- **Кэширование профиля** (30 минут)
- **Batch запросы** к БД
- **Индексы** на `genre`, `artist`, `playCount`
- **Limit x2** при выборке (запас для фильтрации)

### Мониторинг

```javascript
console.log(`🌊 Генерация волны: ${tracks.length} треков`);
console.log(`   Знакомые: ${familiar}, Похожие: ${similar}`);
console.log(`   Открытия: ${discovery}, Популярное: ${trending}`);
```

## Безопасность

- ✅ Требуется авторизация (JWT token)
- ✅ Проверка прав доступа к трекам (`isPublic: true`)
- ✅ Валидация параметров (size, exclude)
- ✅ Rate limiting (TODO)

## Тестирование

### Ручное тестирование

1. **Новый пользователь:**
   - Должен получить fallback (популярные треки)
   - Показать подсказку о недостатке данных

2. **Опытный пользователь:**
   - Разнообразие категорий
   - Нет дубликатов
   - Релевантность жанрам

3. **Бесконечная прокрутка:**
   - Загрузка при скролле
   - Индикатор загрузки
   - Корректная фильтрация (exclude)

### Unit тесты (TODO)

```javascript
describe('MyWaveService', () => {
  it('should generate wave with correct balance', async () => {
    const wave = await myWaveService.generateWave(userId, 20);
    expect(wave.tracks.length).toBe(20);
    expect(wave.metadata.familiar).toBeGreaterThan(0);
  });
});
```

## Примеры

### Запрос следующей порции

```javascript
const response = await axios.get('/api/music/my-wave', {
  headers: { Authorization: `Bearer ${token}` },
  params: {
    size: 20,
    exclude: loadedTrackIds.join(',')
  }
});
```

### Отображение статистики

```jsx
{stats && !stats.fallback && (
  <div className="grid grid-cols-4 gap-4">
    <StatCard 
      label="Знакомые" 
      count={stats.familiar} 
      color="green"
    />
    {/* ... */}
  </div>
)}
```

## Поддержка

При возникновении проблем:

1. Проверьте консоль браузера (F12)
2. Проверьте логи бэкенда (`docker logs errorparty_backend`)
3. Убедитесь, что пользователь авторизован
4. Проверьте наличие истории прослушиваний/лайков

---

**Создано:** 23 декабря 2025  
**Версия:** 1.0.0  
**Статус:** ✅ Работает
