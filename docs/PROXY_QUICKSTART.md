# Настройка прокси для загрузки музыки из ВК - Краткая инструкция

## ✅ Система прокси настроена!

### Что сделано:

1. **Proxy Manager Service** - система ротации прокси
   - Поддержка HTTP/HTTPS и SOCKS4/SOCKS5
   - Автоматическая ротация каждую минуту
   - Проверка работоспособности
   - Статистика использования

2. **Интеграция с VK Service** - прокси для всех запросов к ВК
   - Автоматическая смена при ошибках
   - Умные задержки между запросами

3. **Скрипты для работы**:
   - `test-proxy-system.js` - проверка прокси
   - `fetch-free-proxies.js` - поиск бесплатных прокси
   - `fill-vk-with-proxy.js` - загрузка музыки через прокси

## 🚀 Быстрый старт

### Вариант 1: С бесплатными прокси (ненадежно)

```bash
# Найти рабочие прокси
docker exec errorparty_backend node /app/fetch-free-proxies.js

# Включить в .env
echo "USE_PROXY=true" >> .env
echo "PROXY_LIST=http://proxy1:port,http://proxy2:port" >> .env

# Перезапустить контейнер
docker-compose restart backend

# Попробовать загрузить
docker exec errorparty_backend node /app/fill-vk-with-proxy.js
```

### Вариант 2: С платными прокси (рекомендуется)

#### A. Smartproxy (~$75/мес)
```env
USE_PROXY=true
PROXY_LIST=http://username:password@gate.smartproxy.com:7000
```

#### B. ProxyRack (~$50/мес)
```env
USE_PROXY=true
PROXY_LIST=http://username:password@residential.proxyrack.net:10000
```

#### C. IPRoyal (~$1.75/GB)
```env
USE_PROXY=true
PROXY_LIST=http://username:password@geo.iproyal.com:12321
```

### Вариант 3: Без прокси (использовать существующую музыку)

Система уже работает! У вас:
- **4202 трека** с URL
- **143 альбома** заполнены
- Музыка готова к использованию

## 📋 Команды

### Проверка прокси
```bash
docker exec errorparty_backend node /app/test-proxy-system.js
```

### Поиск бесплатных прокси
```bash
docker exec errorparty_backend node /app/fetch-free-proxies.js
```

### Загрузка с прокси
```bash
docker exec errorparty_backend node /app/fill-vk-with-proxy.js
```

### Статистика музыки
```bash
docker exec errorparty_backend node /app/check-music-stats.js
```

## ⚠️ Важно

**Бесплатные прокси:**
- ❌ Часто не работают (95%+ отказов)
- ❌ Медленные
- ❌ Ненадежные
- ❌ Могут быть уже забанены ВК

**Платные прокси:**
- ✅ Стабильные
- ✅ Быстрые
- ✅ Резидентные IP (как настоящие пользователи)
- ✅ Поддержка

## 🎯 Рекомендация

**Если бюджет ограничен:**
1. Используйте существующие 4202 трека
2. Настройте альтернативные источники (Яндекс.Музыка, Last.fm)
3. Получите новый VK токен с другого аккаунта

**Если нужна ВК музыка:**
1. Купите подписку на Smartproxy ($75/мес)
2. Настройте ротацию прокси
3. Запустите загрузку с задержками 5-10 секунд

## 📚 Полная документация

См. `PROXY_SETUP_GUIDE.md` для подробных инструкций.
