# 🎵 Загрузка полных треков из VK Music

## ⚠️ ВАЖНО: Настрой свои данные VK

Отредактируй файл `backend/load-vk-full-music.js`:

```javascript
const VK_LOGIN = '+79620093157';  // ТВОЙ номер телефона, email или ID
const VK_PASSWORD = 'FantumRaking2!'; // ТВОЙ пароль от VK
```

**Важные моменты:**
- Можно использовать **отдельный пароль приложения** (создается в настройках VK → Безопасность → Пароли приложений)
- Поддерживается **2FA** (если включена двухфакторная авторизация)
- Логин может быть: телефон (+79123456789), email (user@mail.ru) или ID (id12345678)

## 🚀 Запуск загрузки

### Вариант 1: Через Docker (безопаснее)

```powershell
# 1. Скопировать файл в контейнер
docker cp "d:\МОЙ САЙТ\backend\load-vk-full-music.js" errorparty_backend:/app/

# 2. Запустить загрузку
docker exec errorparty_backend node /app/load-vk-full-music.js
```

### Вариант 2: Локально (быстрее)

```powershell
cd "d:\МОЙ САЙТ\backend"
node load-vk-full-music.js
```

## 📊 Что будет загружено

1. **Топ-100 популярных треков** из VK Music
2. **~35 поисковых запросов**:
   - Русские исполнители: Элджей, Miyagi, Скриптонит, Баста, Oxxxymiron и др.
   - Западные хиты: The Weeknd, Dua Lipa, Drake, Taylor Swift и др.
   - Топ треки 2025: Flowers, Kill Bill, Anti-Hero, Unholy и др.
   - Жанры: phonk, russian rap, reggaeton, rock, electronic, pop

**Ожидается:** 500-800 полных треков (не превью!)

## 🎯 Результат

После выполнения будет создан плейлист:
- **🔥 Топ чарт 2025 (VK Music)** - топ 100 треков по популярности

## 🔧 Технические детали

### Как это работает:

1. **Python контейнер** (`vk-token-getter`) получает VK Audio токен через библиотеку `vkaudiotoken`
   - Эмулирует Kate Mobile приложение
   - Обходит капчу (не нужна при получении токена)
   - Возвращает токен + User-Agent

2. **Node.js скрипт** использует полученный токен для:
   - `audio.getPopular` - топ популярных треков
   - `audio.search` - поиск по исполнителям и жанрам
   - Сохранение в PostgreSQL базу данных

### Преимущества над предыдущими попытками:

✅ **Полные треки** (не 30-сек превью как iTunes/theroyakash)  
✅ **Без капчи** при получении токена (только логин/пароль)  
✅ **Без 2Captcha** (не нужен, капчи нет)  
✅ **Официальный VK Audio API** (методы Kate Mobile)  
✅ **Поддержка 2FA** (SMS коды если включены)

## ❓ Возможные проблемы

### "Invalid arguments" или "Failed to get VK token"
- Проверь, что заменил `VK_LOGIN` и `VK_PASSWORD` на свои данные
- Проверь формат логина: +79123456789 (с +7), email или id12345678

### "vkaudiotoken not installed"
- Пересобери Docker образ: `docker build -f backend/Dockerfile.vktoken -t vk-token-getter backend/`

### VK просит SMS код (2FA)
- Библиотека поддерживает 2FA, но нужно будет ввести код вручную
- Альтернатива: создай отдельный пароль приложения в настройках VK (без 2FA)

### "Captcha required" при вызове audio.search
- Это значит токен не Kate Mobile, а обычный
- Убедись, что `vk-token-getter` работает и возвращает правильный токен

## 🔐 Безопасность

- **НЕ коммить** файл с логином/паролем в Git!
- Используй **пароль приложения** вместо основного пароля
- Токен хранится только в памяти (не сохраняется на диск)
- Python контейнер запускается с `--rm` (удаляется после получения токена)

## 📝 Примечания

- Задержка между запросами: 500ms (чтобы не забанили)
- API версия: 5.131 (Kate Mobile)
- Все треки дедуплицируются по artist + title
- Треки с `audio_api_unavailable` фильтруются автоматически
